const {Tag, ItemTag} = require('../models')

class ControllerTag {
    // static tag(req, res) {
    //     ItemTag({ include: { all: true, nested: true }, where: { TagId: req.params.tagId } })
    //     .then((itemtags) => {
            
    //     })
    // }

}

module.exports = ControllerTag;